<header class="fixed top-0 left-0 right-0 z-50 px-4 sm:px-6 lg:px-8 bg-white shadow-md">
  <div class="max-w-7xl mx-auto flex justify-between h-16 items-center">
    <!-- Logo / Nome -->
    <div class="text-lg font-bold text-gray-800">
      <%= gettext("Nosso Contador") %>
    </div>

    <!-- Menu -->
    <nav class="flex space-x-8">
      <.link navigate={~p"/?locale=#{@locale}"} class="text-gray-700 hover:text-blue-600 font-medium">
        <%= gettext("Início") %>
      </.link>
      <.link navigate={~p"/contador?locale=#{@locale}"} class="text-gray-700 hover:text-blue-600 font-medium">
        <%= gettext("Contador") %>
      </.link>
    </nav>

    <!-- Botão trocar idioma -->
    <button
      phx-click="toggle_locale"
      class="ml-4 px-3 py-1 bg-gray-100 hover:bg-gray-200 rounded"
      aria-label={gettext("Toggle Language")}
    >
      <%= if @locale == "pt" do %>
        🇵🇹
      <% else %>
        🇺🇸
      <% end %>
      <span class="ml-1 font-semibold"><%= String.upcase(@locale || "pt") %></span>
    </button>

  </div>
</header>

<main class="pt-20 px-4 py-6 sm:px-6 lg:px-8">
  <div class="mx-auto max-w-2xl">
    <.flash_group flash={@flash} />
    {@inner_content}
  </div>
</main>
